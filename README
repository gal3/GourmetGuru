1.Create (MySql) database with the following name and password

mysql> create database project;
mysql> grant all privileges on project.* to cake@localhost identified by 'cakephp';

2.Dump existing data into the database with the following script

mysql -ucake -pcakephp project < scripts/project_0.51_full.sql 

3.Configure the necessary apache files (sites-enabled) and .htaccess file

4.Configure possible folder permissions

Username/Password for site:
customer/customer
owner/owner
